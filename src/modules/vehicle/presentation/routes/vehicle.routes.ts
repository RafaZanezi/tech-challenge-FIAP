import express from 'express';

const routerVehicles = express.Router();

// routerVehicles.get('/veiculos', VehicleController.read);
// routerVehicles.get('/veiculos/:id', VehicleController.read);

// routerVehicles.post('/veiculos', VehicleController.create);
// routerVehicles.put('/veiculos/:id', VehicleController.update);

// routerVehicles.delete('/veiculos/:id', VehicleController.delete);

export default routerVehicles;
